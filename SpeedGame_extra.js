/* for (let circle of circles) {
    circle.addEventListener("click", activeCircle);
  } */


  /* let rand = []
let  randomCircle = circles[Math.floor(Math.random()*circles.length)];
rand.push(randomCircle);
for (let counter = 0; counter < circles.length; counter++) {
  let newCircle = circles[Math.floor(Math.random()*circles.length)];
  while (rand.lastIndexOf(newCircle) !== -1) {
    newCircle = circles[Math.floor(Math.random()*circles.length)];
  }
  rand.push(newCircle);
}
console.log(rand); */
/* for (let i = circles; i <= circles.length; i++) {
  let rand = [];
  console.log(circles);
  let  randomCircle = circles[Math.floor(Math.random()*circles.length)];
  if (rand.indexOf(randomCircle) == -1) {
    rand.push(randomCircle)
  } else {
    i--;
  }
  console.log(rand);
}
}
random(circles); */
/*     console.log(circles);
  let rand = [];
  for (let i = 0; i <= circles.length; i++) {
    let  randomCircle = circles[Math.floor(Math.random()*(circles.length))];
    let newCircle = rand.indexOf(randomCircle);
    if(newCircle === -1) {
      rand.push(randomCircle)
    }
  }

  console.log(rand);
  return rand; */